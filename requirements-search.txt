# ============================================================================
# CardedAI Optional: Meilisearch 全文搜索
# 未启用时自动降级为内置 SQLAlchemy 搜索。
# 安装：pip install -r requirements-search.txt
# ============================================================================
meilisearch-python-sdk==7.2.1
