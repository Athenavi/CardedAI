# ============================================================================
# CardedAI Optional: Redis 缓存 / 任务队列
# 本机检测到 Redis 时由 run.sh 推荐启用；未安装时自动降级为内存缓存。
# 安装：pip install -r requirements-redis.txt
# ============================================================================
redis==8.0.0
